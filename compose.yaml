services:
  control:
    build: control_Dockerfile
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock  # Docker access
    networks:
      - ansible_net
    
  managed1:
    build: managed_Dockerfile
    networks:
      - ansible_net
      
  managed2:
    build: managed_Dockerfile
    networks:
      - ansible_net

networks:
  ansible_net:
    driver: bridge